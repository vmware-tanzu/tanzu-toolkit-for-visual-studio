<Window x:Class="Tanzu.Toolkit.VisualStudio.WpfViews.ErrorDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tanzu.Toolkit.VisualStudio.WpfViews"
        mc:Ignorable="d"
        Title="{Binding Title}" 
        Height="280" Width="372"
        Name="ErrorDialogViewElement"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow">



    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="200"></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" Margin="0,0,0,10" x:Name="OutputScrollViewer" Background="White" VerticalScrollBarVisibility="Auto">
            <TextBox
                Text="{Binding Message}"
                IsReadOnly="True"
                />
        </ScrollViewer>

        <Button Grid.Row="1" Content="Close" Width="100" HorizontalAlignment="Center"
                Command="{Binding CloseCommand, ElementName=ErrorDialogViewElement}" 
                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
          />
       
    </Grid>
</Window>
